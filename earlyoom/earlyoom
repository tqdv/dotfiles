# Settings for earlyoom. This file is sourced by /bin/sh from
# /etc/init.d/earlyoom or by systemd from earlyoom.service.

# Based on a system with 7.7 GiB of RAM, 2% is around 150 MiB.

EARLYOOM_ARGS="-r 60 -m 2,1 -s 100,100"
